{
  "name": "nikolaostolis.com",
  "version": "1.0.0",
  "description": "Personal website and professional portfolio for Nikolaos Tolis.",
  "main": "index.js",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run format:check && npm run build",
    "deploy": "npm run build && firebase deploy --only hosting --project nikolaos-tolis-site",
    "deploy:preview": "npm run build && firebase hosting:channel:deploy devcontainer-test --expires 7d --project nikolaos-tolis-site",
    "deploy:preview:pr": "npm run build && firebase hosting:channel:deploy pr-local-test --expires 7d --project nikolaos-tolis-site",
    "readme:structure": "node scripts/update-readme-structure.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/niktolis/nikolaostolis.com.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/niktolis/nikolaostolis.com/issues"
  },
  "homepage": "https://github.com/niktolis/nikolaostolis.com#readme",
  "dependencies": {
    "astro": "^6.4.4"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "prettier-plugin-astro": "^0.14.1"
  }
}
